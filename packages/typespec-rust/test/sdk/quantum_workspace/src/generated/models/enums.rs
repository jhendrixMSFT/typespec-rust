// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

/// The type of identity that created the item.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CreatedByType {
    /// The item is created by application
    Application,

    /// The item is created using key
    Key,

    /// The item is created using managed identity
    ManagedIdentity,

    /// The item is created by user
    User,

    /// Any other value not defined in `CreatedByType`.
    UnknownValue(String),
}

/// The scope at which the quota is applied to.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DimensionScope {
    /// The quota is applied to the Azure Subscription.
    Subscription,

    /// The quota is applied to the Quantum Workspace.
    Workspace,

    /// Any other value not defined in `DimensionScope`.
    UnknownValue(String),
}

/// The type of the workspace item.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ItemType {
    /// A program, problem, or application submitted for processing.
    Job,

    /// A logical grouping of jobs.
    Session,

    /// Any other value not defined in `ItemType`.
    UnknownValue(String),
}

/// The status of the job.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobStatus {
    /// Cancellation of the job has been requested.
    CancellationRequested,

    /// The job was cancelled.
    Cancelled,

    /// The job is in the process of being cancelled.
    Cancelling,

    /// The job completed.
    Completed,

    /// The job is being executed.
    Executing,

    /// The job completed with failure.
    Failed,

    /// The job is in the process of being finished.
    Finishing,

    /// The job has been queued.
    Queued,

    /// The job completed with success.
    Succeeded,

    /// The job is waiting in the queue to be executed.
    Waiting,

    /// Any other value not defined in `JobStatus`.
    UnknownValue(String),
}

/// The type of the job.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobType {
    /// Optimization job type.
    Optimization,

    /// Quantum Computing job type.
    QuantumComputing,

    /// Unknown job type.
    Unknown,

    /// Any other value not defined in `JobType`.
    UnknownValue(String),
}

/// The time period in which the quota's underlying meter is accumulated. Based on calendar year. 'None' is used for concurrent
/// quotas.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MeterPeriod {
    /// The meter period is per month.
    Monthly,

    /// The meter period is instantaneous. Used for concurrent quotas.
    None,

    /// Any other value not defined in `MeterPeriod`.
    UnknownValue(String),
}

/// Job priority levels.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Priority {
    /// The job's priority is elevated.
    High,

    /// The job's base priority.
    Standard,

    /// Any other value not defined in `Priority`.
    UnknownValue(String),
}

/// Provider availability.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ProviderAvailability {
    /// Provider is available.
    Available,

    /// Provider is available with degraded experience.
    Degraded,

    /// Provider is unavailable.
    Unavailable,

    /// Any other value not defined in `ProviderAvailability`.
    UnknownValue(String),
}

/// Policy controlling the behavior of the Session when a job in the session fails.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SessionJobFailurePolicy {
    /// New jobs submitted after a job fails will be rejected.
    Abort,

    /// New jobs submitted after a job fails will be accepted.
    Continue,

    /// Any other value not defined in `SessionJobFailurePolicy`.
    UnknownValue(String),
}

/// The status of the session.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SessionStatus {
    /// The session is being executed.
    Executing,

    /// The session completed with failure.
    Failed,

    /// The session completed with some failures.
    FailureS,

    /// The session completed with success.
    Succeeded,

    /// The session timed out.
    TimedOut,

    /// The session is waiting in the queue to be executed.
    Waiting,

    /// Any other value not defined in `SessionStatus`.
    UnknownValue(String),
}

/// Target availability.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TargetAvailability {
    /// Target is available.
    Available,

    /// Target is available with degraded experience.
    Degraded,

    /// Target is unavailable.
    Unavailable,

    /// Any other value not defined in `TargetAvailability`.
    UnknownValue(String),
}
