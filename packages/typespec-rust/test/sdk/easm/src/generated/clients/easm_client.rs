// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use azure_core::{Result, credentials::TokenCredential, error::CheckSuccessOptions, fmt::SafeDebug, http::{ClientOptions, Method, NoFormat, Pager, Pipeline, PipelineSendOptions, RawResponse, Request, RequestContent, Response, Url, UrlExt, pager::{PagerContinuation, PagerResult, PagerState, }, policies::{Policy, auth::BearerTokenAuthorizationPolicy, }, }, json, tracing, };
use crate::generated::models::{AssetChainRequest, AssetChainSummaryResult, AssetResource, AssetUpdateData, AssetsExportRequest, CisaCveResult, DataConnection, DataConnectionData, DeltaDetailsRequest, DeltaPageResult, DeltaSummaryRequest, DeltaSummaryResult, DiscoGroup, DiscoGroupData, DiscoRunPageResult, DiscoTemplate, EasmClientCancelTaskOptions, EasmClientCreateOrReplaceDataConnectionOptions, EasmClientCreateOrReplaceDiscoGroupOptions, EasmClientCreateOrReplacePolicyOptions, EasmClientCreateOrReplaceSavedFilterOptions, EasmClientDeleteDataConnectionOptions, EasmClientDeleteDiscoGroupOptions, EasmClientDeletePolicyOptions, EasmClientDeleteSavedFilterOptions, EasmClientDismissAssetChainOptions, EasmClientDownloadTaskOptions, EasmClientGetAssetChainSummaryOptions, EasmClientGetAssetResourceOptions, EasmClientGetAssetsExportOptions, EasmClientGetBillableOptions, EasmClientGetCisaCveOptions, EasmClientGetDataConnectionOptions, EasmClientGetDeltaSummaryOptions, EasmClientGetDiscoGroupOptions, EasmClientGetDiscoTemplateOptions, EasmClientGetObservationsOptions, EasmClientGetPolicyOptions, EasmClientGetSavedFilterOptions, EasmClientGetSnapshotExportOptions, EasmClientGetSnapshotOptions, EasmClientGetSummaryOptions, EasmClientGetTaskOptions, EasmClientListAssetResourceOptions, EasmClientListCisaCvesOptions, EasmClientListDataConnectionOptions, EasmClientListDeltaDetailsOptions, EasmClientListDiscoGroupOptions, EasmClientListDiscoTemplateOptions, EasmClientListPolicyOptions, EasmClientListRunsOptions, EasmClientListSavedFilterOptions, EasmClientListTaskOptions, EasmClientRunDiscoGroupOptions, EasmClientRunTaskOptions, EasmClientUpdateAssetsOptions, EasmClientValidateDataConnectionOptions, EasmClientValidateDiscoGroupOptions, ObservationPageResult, PagedAssetResource, PagedCisaCveResult, PagedDataConnection, PagedDiscoGroup, PagedDiscoTemplate, PagedPolicy, PagedSavedFilter, PagedTask, Policy, ReportAssetSnapshotExportRequest, ReportAssetSnapshotRequest, ReportAssetSnapshotResult, ReportAssetSummaryRequest, ReportAssetSummaryResult, ReportBillableAssetSummaryResult, SavedFilter, SavedFilterData, Task, ValidateResult, };
use std::sync::Arc;

#[tracing::client]
pub struct EasmClient {
    pub(crate) api_version: String,
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`EasmClient`](EasmClient)
#[derive(Clone, SafeDebug)]
pub struct EasmClientOptions {
/// The API version to use for this operation.
    pub api_version: String,
/// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl EasmClient {
    /// Creates a new EasmClient, using Entra ID authentication.
    ///
    /// # Arguments
    ///
/// * `endpoint` - Service host
/// * `credential` - An implementation of [`TokenCredential`](azure_core::credentials::TokenCredential) that can provide an
///   Entra ID token to use when authenticating.
/// * `options` - Optional configuration for the client.
    #[tracing::new("Customizations.EasmClient")]
    pub fn new(endpoint: &str, credential: Arc<dyn TokenCredential>, options: Option<EasmClientOptions>) -> Result<Self> {
    let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, format!("{endpoint} must use http(s)")));
        }        let auth_policy: Arc<dyn Policy> = Arc::new(BearerTokenAuthorizationPolicy::new(credential, vec!["https://easm.defender.microsoft.com/.default"]));
        Ok(Self {
            endpoint,
            api_version: options.api_version,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                vec![auth_policy], None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Cancel a task by taskId.
    ///
    /// # Arguments
    ///
/// * `task_id` - The unique identifier of the task.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.cancelTask")]
    pub async fn cancel_task(&self, task_id: &str, options: Option<EasmClientCancelTaskOptions<'_>>) -> Result<Response<Task>> {
        if task_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter task_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/tasks/{taskId}:cancel");
        path = path.replace("{taskId}", task_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Create or replace a data connection with a given dataConnectionName.
    ///
    /// # Arguments
    ///
/// * `data_connection_name` - The caller provided unique name for the resource.
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.createOrReplaceDataConnection")]
    pub async fn create_or_replace_data_connection(&self, data_connection_name: &str, body: RequestContent<DataConnectionData>, options: Option<EasmClientCreateOrReplaceDataConnectionOptions<'_>>) -> Result<Response<DataConnection>> {
        if data_connection_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter data_connection_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/dataConnections/{dataConnectionName}");
        path = path.replace("{dataConnectionName}", data_connection_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Put);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Create a discovery group with a given groupName.
    ///
    /// # Arguments
    ///
/// * `group_name` - The caller provided unique name for the resource.
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.createOrReplaceDiscoGroup")]
    pub async fn create_or_replace_disco_group(&self, group_name: &str, body: RequestContent<DiscoGroupData>, options: Option<EasmClientCreateOrReplaceDiscoGroupOptions<'_>>) -> Result<Response<DiscoGroup>> {
        if group_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter group_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/discoGroups/{groupName}");
        path = path.replace("{groupName}", group_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Put);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Create a policy with a given policyName.
    ///
    /// # Arguments
    ///
/// * `policy_name` - The caller provided unique name for the resource.
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.createOrReplacePolicy")]
    pub async fn create_or_replace_policy(&self, policy_name: &str, body: RequestContent<Policy>, options: Option<EasmClientCreateOrReplacePolicyOptions<'_>>) -> Result<Response<Policy>> {
        if policy_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter policy_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/policies/{policyName}");
        path = path.replace("{policyName}", policy_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Put);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Create or replace a saved filter with a given filterName.
    ///
    /// # Arguments
    ///
/// * `filter_name` - The caller provided unique name for the resource.
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.createOrReplaceSavedFilter")]
    pub async fn create_or_replace_saved_filter(&self, filter_name: &str, body: RequestContent<SavedFilterData>, options: Option<EasmClientCreateOrReplaceSavedFilterOptions<'_>>) -> Result<Response<SavedFilter>> {
        if filter_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter filter_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/savedFilters/{filterName}");
        path = path.replace("{filterName}", filter_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Put);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Delete a data connection with a given dataConnectionName.
    ///
    /// # Arguments
    ///
/// * `data_connection_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.deleteDataConnection")]
    pub async fn delete_data_connection(&self, data_connection_name: &str, options: Option<EasmClientDeleteDataConnectionOptions<'_>>) -> Result<Response<(), NoFormat>> {
        if data_connection_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter data_connection_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/dataConnections/{dataConnectionName}");
        path = path.replace("{dataConnectionName}", data_connection_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Delete);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[204]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Delete a discovery group with a given discovery group name.
    ///
    /// # Arguments
    ///
/// * `group_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.deleteDiscoGroup")]
    pub async fn delete_disco_group(&self, group_name: &str, options: Option<EasmClientDeleteDiscoGroupOptions<'_>>) -> Result<Response<(), NoFormat>> {
        if group_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter group_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/discoGroups/{groupName}");
        path = path.replace("{groupName}", group_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Delete);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[204]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Delete a policy with a given policyName.
    ///
    /// # Arguments
    ///
/// * `policy_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.deletePolicy")]
    pub async fn delete_policy(&self, policy_name: &str, options: Option<EasmClientDeletePolicyOptions<'_>>) -> Result<Response<(), NoFormat>> {
        if policy_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter policy_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/policies/{policyName}");
        path = path.replace("{policyName}", policy_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Delete);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[204]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Delete a saved filter with a given filterName.
    ///
    /// # Arguments
    ///
/// * `filter_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.deleteSavedFilter")]
    pub async fn delete_saved_filter(&self, filter_name: &str, options: Option<EasmClientDeleteSavedFilterOptions<'_>>) -> Result<Response<(), NoFormat>> {
        if filter_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter filter_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/savedFilters/{filterName}");
        path = path.replace("{filterName}", filter_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Delete);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[204]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Dismiss discovery chain for a given asset chain source
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.dismissAssetChain")]
    pub async fn dismiss_asset_chain(&self, body: RequestContent<AssetChainRequest>, options: Option<EasmClientDismissAssetChainOptions<'_>>) -> Result<Response<Task>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/discoGroups:dismissAssetChain");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Download a task.
    ///
    /// # Arguments
    ///
/// * `task_id` - The unique identifier of the task.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.downloadTask")]
    pub async fn download_task(&self, task_id: &str, options: Option<EasmClientDownloadTaskOptions<'_>>) -> Result<Response<Task>> {
        if task_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter task_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/tasks/{taskId}:download");
        path = path.replace("{taskId}", task_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve an asset chain summary.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getAssetChainSummary")]
    pub async fn get_asset_chain_summary(&self, body: RequestContent<AssetChainRequest>, options: Option<EasmClientGetAssetChainSummaryOptions<'_>>) -> Result<Response<AssetChainSummaryResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/discoGroups:getAssetChainSummary");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve an asset by assetId.
    ///
    /// # Arguments
    ///
/// * `asset_id` - The system generated unique id for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getAssetResource")]
    pub async fn get_asset_resource(&self, asset_id: &str, options: Option<EasmClientGetAssetResourceOptions<'_>>) -> Result<Response<AssetResource>> {
        if asset_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter asset_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/assets/{assetId}");
        path = path.replace("{assetId}", asset_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Export a list of assets for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getAssetsExport")]
    pub async fn get_assets_export(&self, body: RequestContent<AssetsExportRequest>, options: Option<EasmClientGetAssetsExportOptions<'_>>) -> Result<Response<Task>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/assets:export");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(orderby) = options.orderby.as_ref() {
            query_builder.set_pair("orderby", orderby);
        }
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Get billable assets summary for the workspace.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getBillable")]
    pub async fn get_billable(&self, options: Option<EasmClientGetBillableOptions<'_>>) -> Result<Response<ReportBillableAssetSummaryResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/reports/assets:getBillable");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve details of CisaCve by cveId
    ///
    /// # Arguments
    ///
/// * `cve_id` - The CVE ID of the vulnerability in the format CVE-YYYY-NNNN, note that the number portion can have more than
///   4 digits.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getCisaCve")]
    pub async fn get_cisa_cve(&self, cve_id: &str, options: Option<EasmClientGetCisaCveOptions<'_>>) -> Result<Response<CisaCveResult>> {
        if cve_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter cve_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/cisaCves/{cveId}");
        path = path.replace("{cveId}", cve_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a data connection with a given dataConnectionName.
    ///
    /// # Arguments
    ///
/// * `data_connection_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getDataConnection")]
    pub async fn get_data_connection(&self, data_connection_name: &str, options: Option<EasmClientGetDataConnectionOptions<'_>>) -> Result<Response<DataConnection>> {
        if data_connection_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter data_connection_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/dataConnections/{dataConnectionName}");
        path = path.replace("{dataConnectionName}", data_connection_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a list of deltas with overall summary changes for the provided time range.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getDeltaSummary")]
    pub async fn get_delta_summary(&self, body: RequestContent<DeltaSummaryRequest>, options: Option<EasmClientGetDeltaSummaryOptions<'_>>) -> Result<Response<DeltaSummaryResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/assets:getDeltaSummary");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a discovery group with a given groupName.
    ///
    /// # Arguments
    ///
/// * `group_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getDiscoGroup")]
    pub async fn get_disco_group(&self, group_name: &str, options: Option<EasmClientGetDiscoGroupOptions<'_>>) -> Result<Response<DiscoGroup>> {
        if group_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter group_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/discoGroups/{groupName}");
        path = path.replace("{groupName}", group_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a disco template with a given templateId.
    ///
    /// # Arguments
    ///
/// * `template_id` - The system generated unique id for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getDiscoTemplate")]
    pub async fn get_disco_template(&self, template_id: &str, options: Option<EasmClientGetDiscoTemplateOptions<'_>>) -> Result<Response<DiscoTemplate>> {
        if template_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter template_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/discoTemplates/{templateId}");
        path = path.replace("{templateId}", template_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve observations on an asset
    ///
    /// # Arguments
    ///
/// * `asset_id` - The system generated unique id for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getObservations")]
    pub async fn get_observations(&self, asset_id: &str, options: Option<EasmClientGetObservationsOptions<'_>>) -> Result<Response<ObservationPageResult>> {
        if asset_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter asset_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/assets/{assetId}:getObservations");
        path = path.replace("{assetId}", asset_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(orderby) = options.orderby.as_ref() {
            query_builder.set_pair("orderby", orderby);
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a policy with a given policyName.
    ///
    /// # Arguments
    ///
/// * `policy_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getPolicy")]
    pub async fn get_policy(&self, policy_name: &str, options: Option<EasmClientGetPolicyOptions<'_>>) -> Result<Response<Policy>> {
        if policy_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter policy_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/policies/{policyName}");
        path = path.replace("{policyName}", policy_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a saved filter by filterName.
    ///
    /// # Arguments
    ///
/// * `filter_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getSavedFilter")]
    pub async fn get_saved_filter(&self, filter_name: &str, options: Option<EasmClientGetSavedFilterOptions<'_>>) -> Result<Response<SavedFilter>> {
        if filter_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter filter_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/savedFilters/{filterName}");
        path = path.replace("{filterName}", filter_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Get the most recent snapshot of asset summary values for the snapshot request.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getSnapshot")]
    pub async fn get_snapshot(&self, body: RequestContent<ReportAssetSnapshotRequest>, options: Option<EasmClientGetSnapshotOptions<'_>>) -> Result<Response<ReportAssetSnapshotResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/reports/assets:getSnapshot");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Get the most recent snapshot of asset summary values for the snapshot request exported to a file.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getSnapshotExport")]
    pub async fn get_snapshot_export(&self, body: RequestContent<ReportAssetSnapshotExportRequest>, options: Option<EasmClientGetSnapshotExportOptions<'_>>) -> Result<Response<Task>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/reports/assets:getSnapshotExport");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Get asset summary details for the summary request.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getSummary")]
    pub async fn get_summary(&self, body: RequestContent<ReportAssetSummaryRequest>, options: Option<EasmClientGetSummaryOptions<'_>>) -> Result<Response<ReportAssetSummaryResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/reports/assets:getSummary");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a task by taskId.
    ///
    /// # Arguments
    ///
/// * `task_id` - The unique identifier of the task.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getTask")]
    pub async fn get_task(&self, task_id: &str, options: Option<EasmClientGetTaskOptions<'_>>) -> Result<Response<Task>> {
        if task_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter task_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/tasks/{taskId}");
        path = path.replace("{taskId}", task_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Retrieve a list of assets for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listAssetResource")]
    pub fn list_asset_resource(&self, options: Option<EasmClientListAssetResourceOptions<'_>>) -> Result<Pager<PagedAssetResource>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/assets");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(mark) = options.mark.as_ref() {
            query_builder.set_pair("mark", mark);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(orderby) = options.orderby.as_ref() {
            query_builder.set_pair("orderby", orderby);
        }
        if let Some(recent_only) = options.recent_only {
            query_builder.set_pair("recentOnly", recent_only.to_string());
        }
        if let Some(response_includes) = options.response_includes.as_ref() {
            query_builder.set_pair("responseIncludes", response_includes.join(","));
        }
        if let Some(response_type) = options.response_type.as_ref() {
            query_builder.set_pair("responseType", response_type.as_ref());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedAssetResource = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of CisaCves for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getCisaCves")]
    pub fn list_cisa_cves(&self, options: Option<EasmClientListCisaCvesOptions<'_>>) -> Result<Pager<PagedCisaCveResult>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/cisaCves");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedCisaCveResult = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of data connections.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listDataConnection")]
    pub fn list_data_connection(&self, options: Option<EasmClientListDataConnectionOptions<'_>>) -> Result<Pager<PagedDataConnection>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/dataConnections");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedDataConnection = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of deltas for the provided time range.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.getDeltaDetails")]
    pub fn list_delta_details(&self, body: RequestContent<DeltaDetailsRequest>, options: Option<EasmClientListDeltaDetailsOptions<'_>>) -> Result<Pager<DeltaPageResult>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/assets:getDeltaDetails");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Post);
            request.insert_header("accept", "application/json");
            request.insert_header("content-type", "application/json");
            request.set_body(body.clone());
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: DeltaPageResult = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of discovery group for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listDiscoGroup")]
    pub fn list_disco_group(&self, options: Option<EasmClientListDiscoGroupOptions<'_>>) -> Result<Pager<PagedDiscoGroup>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/discoGroups");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedDiscoGroup = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of disco templates for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listDiscoTemplate")]
    pub fn list_disco_template(&self, options: Option<EasmClientListDiscoTemplateOptions<'_>>) -> Result<Pager<PagedDiscoTemplate>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/discoTemplates");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedDiscoTemplate = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of policies for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listPolicy")]
    pub fn list_policy(&self, options: Option<EasmClientListPolicyOptions<'_>>) -> Result<Pager<PagedPolicy>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/policies");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedPolicy = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a collection of discovery run results for a discovery group with a given groupName.
    ///
    /// # Arguments
    ///
/// * `group_name` - The unique identifier for the discovery group.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listRuns")]
    pub fn list_runs(&self, group_name: &str, options: Option<EasmClientListRunsOptions<'_>>) -> Result<Pager<DiscoRunPageResult>> {
        if group_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter group_name cannot be empty"));
        }let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        let mut path = String::from("/discoGroups/{groupName}/runs");
        path = path.replace("{groupName}", group_name);
        first_url.append_path(&path);
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: DiscoRunPageResult = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of saved filters for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listSavedFilter")]
    pub fn list_saved_filter(&self, options: Option<EasmClientListSavedFilterOptions<'_>>) -> Result<Pager<PagedSavedFilter>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/savedFilters");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedSavedFilter = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Retrieve a list of tasks for the provided search parameters.
    ///
    /// # Arguments
    ///
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.listTask")]
    pub fn list_task(&self, options: Option<EasmClientListTaskOptions<'_>>) -> Result<Pager<PagedTask>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        first_url.append_path("/tasks");
        let mut query_builder = first_url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(filter) = options.filter.as_ref() {
            query_builder.set_pair("filter", filter);
        }
        if let Some(maxpagesize) = options.maxpagesize {
            query_builder.set_pair("maxpagesize", maxpagesize.to_string());
        }
        if let Some(orderby) = options.orderby.as_ref() {
            query_builder.set_pair("orderby", orderby);
        }
        if let Some(skip) = options.skip {
            query_builder.set_pair("skip", skip.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(move |next_link: PagerState, pager_options| {
            let url = match next_link {
                PagerState::More(next_link) => {
                    let mut next_link: Url = next_link.try_into().expect("expected Url");
                    let mut query_builder = next_link.query_builder();
                    query_builder.set_pair("api-version", &api_version);
                    query_builder.build();
                    next_link
                },
                PagerState::Initial => {
                    first_url.clone()
                },
            };
            let mut request = Request::new(url, Method::Get);
            request.insert_header("accept", "application/json");
            let pipeline = pipeline.clone();
            Box::pin({
                let first_url = first_url.clone();
                async move {
                    let rsp = pipeline.send(&pager_options.context, &mut request, Some(PipelineSendOptions {
                        check_success: CheckSuccessOptions{ success_codes: &[200]},
                        ..Default::default()
                    })).await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: PagedTask = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp, continuation: PagerContinuation::Link(first_url.join(next_link.as_ref())?)
                        },
                        _ => PagerResult::Done {
                            response: rsp
                        },
                    })
                }
            })
            },
            Some(options.method_options),
        ))
    }

    /// Run a discovery group with a given groupName.
    ///
    /// # Arguments
    ///
/// * `group_name` - The caller provided unique name for the resource.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.runDiscoGroup")]
    pub async fn run_disco_group(&self, group_name: &str, options: Option<EasmClientRunDiscoGroupOptions<'_>>) -> Result<Response<(), NoFormat>> {
        if group_name.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter group_name cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/discoGroups/{groupName}:run");
        path = path.replace("{groupName}", group_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[204]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Run a task by taskId.
    ///
    /// # Arguments
    ///
/// * `task_id` - The unique identifier of the task.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.runTask")]
    pub async fn run_task(&self, task_id: &str, options: Option<EasmClientRunTaskOptions<'_>>) -> Result<Response<Task>> {
        if task_id.is_empty() {
            return Err(azure_core::Error::with_message(azure_core::error::ErrorKind::Other, "parameter task_id cannot be empty"));
        }let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/tasks/{taskId}:run");
        path = path.replace("{taskId}", task_id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Update labels on assets matching the provided filter.
    ///
    /// # Arguments
    ///
/// * `filter` - An expression on the resource type that selects the resources to be returned.
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.updateAssets")]
    pub async fn update_assets(&self, filter: &str, body: RequestContent<AssetUpdateData>, options: Option<EasmClientUpdateAssetsOptions<'_>>) -> Result<Response<Task>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/assets");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.set_pair("filter", filter);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Validate a data connection with a given dataConnectionName.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.validateDataConnection")]
    pub async fn validate_data_connection(&self, body: RequestContent<DataConnectionData>, options: Option<EasmClientValidateDataConnectionOptions<'_>>) -> Result<Response<ValidateResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/dataConnections:validate");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }

    /// Validate a discovery group with a given groupName.
    ///
    /// # Arguments
    ///
/// * `body` - Body parameter.
/// * `options` - Optional parameters for the request.
    #[tracing::function("Customizations.EasmClient.validateDiscoGroup")]
    pub async fn validate_disco_group(&self, body: RequestContent<DiscoGroupData>, options: Option<EasmClientValidateDiscoGroupOptions<'_>>) -> Result<Response<ValidateResult>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/discoGroups:validate");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        request.set_body(body);
        let rsp = self.pipeline.send(&ctx, &mut request, Some(PipelineSendOptions {
            check_success: CheckSuccessOptions{ success_codes: &[200]},
            ..Default::default()
        })).await?;
        Ok(rsp.into())

    }
}

/// Default value for [`EasmClientOptions::api_version`].
pub(crate) const DEFAULT_API_VERSION: &str = "2024-10-01-preview";

impl Default for EasmClientOptions {
    fn default() -> Self {
        Self {
            api_version: String::from(DEFAULT_API_VERSION),
            client_options: ClientOptions::default(),
        }
    }
}


