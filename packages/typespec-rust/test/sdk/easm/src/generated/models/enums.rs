// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

    /// A list of asset chain sources
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetChainSource {
        /// asset
        Asset,

        /// discovery group
        DiscoGroup,

        /// Any other value not defined in `AssetChainSource`.
        UnknownValue(String),
    }

    /// The kind of asset.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetKind {
        As,

        Contact,

        Domain,

        Host,

        IPAddress,

        IPBlock,

        Page,

        SslCert,

        /// Any other value not defined in `AssetKind`.
        UnknownValue(String),
    }

    /// A list of asset response type
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetResponseType {
        /// Return the full set of asset fields.
        Full,

        /// Return only the asset id.
        Id,

        /// Return a reduced set of fields.
        Reduced,

        /// Return the standard set of asset fields.
        Standard,

        /// Any other value not defined in `AssetResponseType`.
        UnknownValue(String),
    }

    /// The state of assets
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetState {
        Archived,

        AssociatedPartner,

        AssociatedThirdparty,

        Candidate,

        CandidateInvestigate,

        Confirmed,

        Dismissed,

        /// Any other value not defined in `AssetState`.
        UnknownValue(String),
    }

    /// The state to update the asset to.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetUpdateState {
        AssociatedPartner,

        AssociatedThirdparty,

        Candidate,

        CandidateInvestigate,

        Confirmed,

        Dismissed,

        /// Any other value not defined in `AssetUpdateState`.
        UnknownValue(String),
    }

    /// A list of asset types to cascade the updates to.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AssetUpdateTransfers {
        As,

        Contact,

        Domain,

        Host,

        IPAddress,

        IPBlock,

        Page,

        SslCert,

        /// Any other value not defined in `AssetUpdateTransfers`.
        UnknownValue(String),
    }

    /// The kind of asset.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum AuditTrailItemKind {
        As,

        Contact,

        Domain,

        Host,

        IPAddress,

        IPBlock,

        Page,

        SslCert,

        /// Any other value not defined in `AuditTrailItemKind`.
        UnknownValue(String),
    }

    /// The type of data the data connection will transfer
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum DataConnectionContent {
        Assets,

        AttackSurfaceInsights,

        /// Any other value not defined in `DataConnectionContent`.
        UnknownValue(String),
    }

    /// The rate at which the data connection will receive updates.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum DataConnectionFrequency {
        Daily,

        Monthly,

        Weekly,

        /// Any other value not defined in `DataConnectionFrequency`.
        UnknownValue(String),
    }

    /// A list of delta detail type
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum DeltaDetailType {
        /// added
        Added,

        /// removed
        Removed,

        /// Any other value not defined in `DeltaDetailType`.
        UnknownValue(String),
    }

    /// The State of the disco run.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum DiscoRunState {
        Completed,

        Failed,

        Pending,

        Running,

        /// Any other value not defined in `DiscoRunState`.
        UnknownValue(String),
    }

    /// The kind of disco source.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum DiscoSourceKind {
        As,

        Attribute,

        Contact,

        Domain,

        Host,

        IPBlock,

        /// Any other value not defined in `DiscoSourceKind`.
        UnknownValue(String),
    }

    /// A list of global asset type
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum GlobalAssetType {
        /// autonomous system number
        As,

        /// contact
        Contact,

        /// domain
        Domain,

        /// host
        Host,

        /// ip address
        IPAddress,

        /// ip block
        IPBlock,

        /// mail server
        MailServer,

        /// name server
        NameServer,

        /// page
        Page,

        /// resource
        Resource,

        /// ssl certificate
        SslCert,

        /// Any other value not defined in `GlobalAssetType`.
        UnknownValue(String),
    }

    /// A list of global inventory states.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum GlobalInventoryState {
        /// archived
        Archived,

        /// associated
        Associated,

        /// associated partner
        AssociatedPartner,

        /// associated third party
        AssociatedThirdParty,

        /// auto confirmed
        Autoconfirmed,

        /// candidate
        Candidate,

        /// candidate investigate
        CandidateInvestigate,

        /// confirmed
        Confirmed,

        /// dismissed
        Dismissed,

        /// Any other value not defined in `GlobalInventoryState`.
        UnknownValue(String),
    }

    /// A list of observation priorities
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ObservationPriority {
        /// high
        High,

        /// low
        Low,

        /// medium
        Medium,

        /// none
        None,

        /// Any other value not defined in `ObservationPriority`.
        UnknownValue(String),
    }

    /// A list of observation remediation sources
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ObservationRemediationSource {
        /// The source of the observation state is the system.
        System,

        /// The source of the observation state is a user.
        User,

        /// Any other value not defined in `ObservationRemediationSource`.
        UnknownValue(String),
    }

    /// A list of observation states
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ObservationRemediationState {
        /// active
        Active,

        /// nonApplicable
        NonApplicable,

        /// Any other value not defined in `ObservationRemediationState`.
        UnknownValue(String),
    }

    /// A list of observation types
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ObservationType {
        /// common vulnerabilities and exposures
        Cve,

        /// insight
        Insight,

        /// Any other value not defined in `ObservationType`.
        UnknownValue(String),
    }

    /// state of observed port
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ObservedPortStateValue {
        Closed,

        Filtered,

        Open,

        /// Any other value not defined in `ObservedPortStateValue`.
        UnknownValue(String),
    }

    /// The redirect type of a page
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum PageAssetRedirectType {
        Final,

        HttpHeader,

        Javascript,

        MetaRefresh,

        /// Any other value not defined in `PageAssetRedirectType`.
        UnknownValue(String),
    }

    /// A list of policy actions
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum PolicyAction {
        /// Add a resource label with a given name.
        AddResource,

        /// Remove from inventory.
        RemoveFromInventory,

        /// Remove a resource label with a given name.
        RemoveResource,

        /// Set an external id.
        SetExternalId,

        /// Set inventory state to a given supported state value.
        SetState,

        /// Any other value not defined in `PolicyAction`.
        UnknownValue(String),
    }

    /// The kind of billable asset.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum ReportBillableAssetBreakdownKind {
        Domain,

        Host,

        IPAddress,

        /// Any other value not defined in `ReportBillableAssetBreakdownKind`.
        UnknownValue(String),
    }

    /// The validation type of a Ssl certificate
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum SslCertAssetValidationType {
        DomainValidation,

        ExtendedValidation,

        OrganizationValidation,

        /// Any other value not defined in `SslCertAssetValidationType`.
        UnknownValue(String),
    }

    /// The phase the task is in.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum TaskPhase {
        Complete,

        Polling,

        Running,

        /// Any other value not defined in `TaskPhase`.
        UnknownValue(String),
    }

    /// The state the task is in.
    #[derive(Clone, Debug, Eq, PartialEq)]
    pub enum TaskState {
        Complete,

        Failed,

        Incomplete,

        Paused,

        Pending,

        Running,

        Warning,

        /// Any other value not defined in `TaskState`.
        UnknownValue(String),
    }

