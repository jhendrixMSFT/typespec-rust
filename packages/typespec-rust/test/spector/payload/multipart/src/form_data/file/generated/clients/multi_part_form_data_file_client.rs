// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    form_data::file::generated::models::{
        MultiPartFormDataFileClientUploadFileArrayOptions,
        MultiPartFormDataFileClientUploadFileRequiredFilenameOptions,
        MultiPartFormDataFileClientUploadFileSpecificContentTypeOptions, UploadFileArrayRequest,
        UploadFileRequiredFilenameRequest, UploadFileSpecificContentTypeRequest,
    },
    multipart::{MultipartFormData, Part},
};
use azure_core::{
    error::CheckSuccessOptions,
    http::{Method, NoFormat, Pipeline, PipelineSendOptions, Request, Response, Url, UrlExt},
    tracing, Result,
};

#[tracing::client]
pub struct MultiPartFormDataFileClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl MultiPartFormDataFileClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Upload an array of files.
    ///
    /// # Arguments
    ///
    /// * `body` - Request containing an array of files with `image/png` content type.
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Payload.MultiPart.FormData.File.uploadFileArray")]
    pub async fn upload_file_array(
        &self,
        body: UploadFileArrayRequest,
        options: Option<MultiPartFormDataFileClientUploadFileArrayOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/multipart/form-data/file/file-array");
        let mut request = Request::new(url, Method::Post);

        let mut form = MultipartFormData::new();
        for file in body.files {
            form = form.part("files", Part::from(file));
        }
        request.insert_header("content-type", form.content_type_header());
        request.set_body(form.into_bytes());

        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Upload a file with a required filename.
    ///
    /// # Arguments
    ///
    /// * `body` - Request containing a file with a required filename and `image/png` content type.
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Payload.MultiPart.FormData.File.uploadFileRequiredFilename")]
    pub async fn upload_file_required_filename(
        &self,
        body: UploadFileRequiredFilenameRequest,
        options: Option<MultiPartFormDataFileClientUploadFileRequiredFilenameOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/multipart/form-data/file/required-filename");
        let mut request = Request::new(url, Method::Post);

        let form = MultipartFormData::new().part("file", Part::from(body.file));
        request.insert_header("content-type", form.content_type_header());
        request.set_body(form.into_bytes());

        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Upload a file with a specific content type.
    ///
    /// # Arguments
    ///
    /// * `body` - Request containing a file with `image/png` content type.
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Payload.MultiPart.FormData.File.uploadFileSpecificContentType")]
    pub async fn upload_file_specific_content_type(
        &self,
        body: UploadFileSpecificContentTypeRequest,
        options: Option<MultiPartFormDataFileClientUploadFileSpecificContentTypeOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/multipart/form-data/file/specific-content-type");
        let mut request = Request::new(url, Method::Post);

        let form = MultipartFormData::new().part("file", Part::from(body.file));
        request.insert_header("content-type", form.content_type_header());
        request.set_body(form.into_bytes());

        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
