// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    ListWidgetsResponse, PreviewVersionClientGetWidgetOptions,
    PreviewVersionClientListWidgetsOptions, PreviewVersionClientUpdateWidgetColorOptions,
    UpdateWidgetColorRequest, Widget,
};
use azure_core::{
    error::CheckSuccessOptions,
    fmt::SafeDebug,
    http::{
        ClientOptions, Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response,
        Url, UrlExt,
    },
    tracing, Result,
};

#[tracing::client]
pub struct PreviewVersionClient {
    pub(crate) api_version: String,
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`PreviewVersionClient`](PreviewVersionClient)
#[derive(Clone, SafeDebug)]
pub struct PreviewVersionClientOptions {
    /// The API version to use for this operation.
    pub api_version: String,
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl PreviewVersionClient {
    /// Creates a new PreviewVersionClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("_Specs_.Azure.Versioning.PreviewVersion")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<PreviewVersionClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            api_version: options.api_version,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Get widget by id (available in all versions)
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("_Specs_.Azure.Versioning.PreviewVersion.getWidget")]
    pub async fn get_widget(
        &self,
        id: &str,
        options: Option<PreviewVersionClientGetWidgetOptions<'_>>,
    ) -> Result<Response<Option<Widget>>> {
        if id.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter id cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/azure/versioning/previewVersion/widgets/{id}");
        path = path.replace("{id}", id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200, 404],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// List widgets with optional color filtering
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("_Specs_.Azure.Versioning.PreviewVersion.listWidgets")]
    pub async fn list_widgets(
        &self,
        options: Option<PreviewVersionClientListWidgetsOptions<'_>>,
    ) -> Result<Response<ListWidgetsResponse>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/azure/versioning/previewVersion/widgets");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(color) = options.color.as_ref() {
            query_builder.set_pair("color", color);
        }
        if let Some(name) = options.name.as_ref() {
            query_builder.set_pair("name", name);
        }
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Update widget color (preview only)
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("_Specs_.Azure.Versioning.PreviewVersion.updateWidgetColor")]
    pub async fn update_widget_color(
        &self,
        id: &str,
        color_update: RequestContent<UpdateWidgetColorRequest>,
        options: Option<PreviewVersionClientUpdateWidgetColorOptions<'_>>,
    ) -> Result<Response<Option<Widget>>> {
        if id.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter id cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/azure/versioning/previewVersion/widgets/{id}/color");
        path = path.replace("{id}", id);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Patch);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/merge-patch+json");
        request.set_body(color_update);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200, 404],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}

/// Default value for [`PreviewVersionClientOptions::api_version`].
#[allow(dead_code)]
pub(crate) const DEFAULT_API_VERSION: &str = "2024-12-01-preview";

impl Default for PreviewVersionClientOptions {
    fn default() -> Self {
        Self {
            api_version: String::from(DEFAULT_API_VERSION),
            client_options: ClientOptions::default(),
        }
    }
}
